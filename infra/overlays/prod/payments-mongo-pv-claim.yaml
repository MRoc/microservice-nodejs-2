apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: payments-mongo-pv-claim
spec:
  storageClassName: do-block-storage
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi